const login = () => {
    //login process will be done here;
}
const logout = () => {
    //logout process will be done;
}
const editProfile = () =>{
    // profile editing will be done here
}

module.exports = {login,logout,editProfile};